after executing prompt given by anushka these are the changes
Schema Changes Needed
None. All 35 tables cover the updated frontend.

API Changes Needed
No new endpoints. Still 24. But two backend implementation notes for your dev:

GET /api/v1/dashboard/subordinates — must work for Directors at Media/Design schools (SoMCS, CioD, SoAA). The filtering by school already handles this, just make sure Director at those schools isn't being restricted to standard-form faculty only.

POST /api/v1/appraisal/submit — the grand_total stored in declarations should use the correct max based on form type (620 standard / 555 media / 575 design), not a hardcoded 620.