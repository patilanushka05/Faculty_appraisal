<<<<<<< HEAD
I can see exactly what's wrong. There are 4 bugs in the code you shared:

row_no=None always passed — the ternary always passes the kwarg even when the model doesn't have that column, which crashes SQLAlchemy
item.items() called on a string — innovDetails is a plain string (not a dict/list), so iterating .items() throws AttributeError and rolls back the entire transaction
No type coercion — string values like "5" being set into INTEGER/NUMERIC columns cause DataError from asyncpg on commit
Steps 2 & 3 (Declaration + Snapshot) are outside the try/except — if they throw, the shred_form inserts are silently rolled back with no proper error response
The reason you see "success" is that innovDetails is usually an empty string → not "" is True → the continue skips it. But other numeric fields then fail on commit, rolling back everything. OR the whole commit succeeds but with all NULL data because field names don't match model columns.

Here is the fully corrected shred_form and submit_appraisal:


from sqlalchemy import inspect as sa_inspect
from sqlalchemy.types import Integer as SAInteger, Numeric as SANumeric
from sqlalchemy.orm import flag_modified


def _coerce_for_column(instance, field_name: str, value):
    """Coerce value to the correct Python type for the given SQLAlchemy column."""
=======
The _safe_db_value helper is too aggressive — it converts '2' (string) to 2 (int), then asyncpg rejects an int going into a VARCHAR column. We need column-type-aware coercion.

Replace _safe_db_value and the field-setting loop in shred_form with this:


from sqlalchemy import inspect as sa_inspect
from sqlalchemy import Numeric as SANumeric, Integer as SAInteger, String as SAString


def _coerce_for_column(model_instance, field_name, value):
    """Coerce value to match the actual DB column type."""
>>>>>>> refs/remotes/origin/main
    if value is None:
        return None
    if isinstance(value, str) and value.strip() == '':
        return None
<<<<<<< HEAD
    try:
        col = sa_inspect(type(instance)).columns.get(field_name)
        if col is not None:
            if isinstance(col.type, SAInteger):
                return int(float(value))
            elif isinstance(col.type, SANumeric):
                return float(value)
            else:
=======

    try:
        col = sa_inspect(type(model_instance)).columns.get(field_name)
        if col is not None:
            col_type = col.type
            if isinstance(col_type, SAInteger):
                try:
                    return int(value)
                except (ValueError, TypeError):
                    return None
            elif isinstance(col_type, SANumeric):
                try:
                    return float(value)
                except (ValueError, TypeError):
                    return None
            else:
                # VARCHAR / Text / String — always convert to str
>>>>>>> refs/remotes/origin/main
                if isinstance(value, (int, float)):
                    return str(value)
    except Exception:
        pass
<<<<<<< HEAD
    return value


async def shred_form(db: AsyncSession, email: str, year: str, form_data: Dict[str, Any], form_family: str):
    mappings = {
        "lectures":         (models_a.TeachingProcess,          "A1. Lectures / Tutorials / Practicals"),
        "courseFile":       (models_a.CourseFile,                "A2. Course File"),
        "projects":         (models_a.ProjectGuided,             "A4. Projects"),
        "quals":            (models_a.QualificationEnhancement,  "A5. Qualification Enhancement"),
        "feedback":         (models_a.StudentFeedback,           "Student Feedback"),
        "deptActs":         (models_a.DepartmentActivity,        "Departmental / School Activities"),
        "uniActs":          (models_a.UniversityActivity,        "University Level Activities"),
        "society":          (models_a.SocialContribution,        "Contribution to Society"),
        "industry":         (models_a.IndustryConnect,           "Industry Connect"),
        "acr":              (models_a.ACRScore,                  "Annual Confidential Report - School Level"),
        "journals":         (models_b.JournalPublication,        "B1. Research Papers / Journal Publications"),
        "books":            (models_b.BookPublication,           "B2. Books / Book Chapters"),
        "ict":              (models_b.ICTPedagogy,               "B3. ICT / E-Content / Pedagogy"),
        "research":         (models_b.ResearchGuidance,          "B4(a). Research Guidance - PhD / PG"),
        "projects2":        (models_b.ResearchProject,           "B4(b). Research / Consultancy Internal Projects"),
        "externalProjects": (models_b.ExternalResearchProject,   "B4(c). Research / Consultancy External Projects"),
        "patents":          (models_b.Patent,                    "B5(a). Patents (IPR)"),
        "awards":           (models_b.Award,                     "B5(b). Awards"),
        "confs":            (models_b.Conference,                "B6. Invited Lectures / Paper Presentations"),
        "proposals":        (models_b.ResearchProposal,          "B7(a). Submitted Research Proposals"),
        "products":         (models_b.ProductDeveloped,          "B7(b). Product Developed and Used by Students"),
        "fdps":             (models_b.SelfDevelopment,           "B8(a). FDP / Workshops"),
        "training":         (models_b.IndustrialTraining,        "B8(b). Industrial Training"),
    }

    # ── InnovativeTeaching: scalar strings, NOT a list-of-dicts ──────────────
    await db.execute(delete(models_a.InnovativeTeaching).where(
        models_a.InnovativeTeaching.faculty_email == email,
        models_a.InnovativeTeaching.academic_year == year,
    ))
    innov_details = form_data.get("innovDetails") or ""
    innov_score   = form_data.get("innovScore")   or ""
    if innov_details or innov_score:
        innov = models_a.InnovativeTeaching(
            faculty_email=email,
            academic_year=year,
            form_family=form_family,
            section_title="A3. Innovative Teaching-Learning",
        )
        # Try common column name variations for the details text
        for fname in ("details", "description", "innov_details", "innovDetails"):
            if hasattr(innov, fname):
                setattr(innov, fname, innov_details or None)
                break
        # Try common column name variations for the score
        for fname in ("score", "self_score", "innov_score", "innovScore"):
            if hasattr(innov, fname):
                setattr(innov, fname, _coerce_for_column(innov, fname, innov_score))
                break
        db.add(innov)

    # ── All other sections: list-of-dicts ─────────────────────────────────────
    for key, (model, title) in mappings.items():
        await db.execute(delete(model).where(
            model.faculty_email == email,
            model.academic_year == year,
        ))

        section_data = form_data.get(key)
        if not section_data:
            continue

        items = section_data if isinstance(section_data, list) else [section_data]

        for idx, item in enumerate(items):
            # Skip non-dict items (safeguard)
            if not isinstance(item, dict):
                continue

            # Skip completely blank rows
            if not any(v not in (None, "", 0, "0") for v in item.values()):
                continue

            # Build constructor kwargs — only pass row_no if the model has that column
            kwargs: Dict[str, Any] = dict(
                faculty_email=email,
                academic_year=year,
                form_family=form_family,
                section_title=title,
            )
            if hasattr(model, "row_no"):
                kwargs["row_no"] = idx + 1

            db_item = model(**kwargs)

            for field_name, value in item.items():
                if hasattr(db_item, field_name):
                    try:
                        setattr(db_item, field_name, _coerce_for_column(db_item, field_name, value))
                    except (ValueError, TypeError):
                        pass  # Skip fields whose value can't be coerced

            db.add(db_item)


@router.post("/submit")
async def submit_appraisal(
    data: Dict[str, Any],
    current_user: CurrentUser,
    db: AsyncSession = Depends(get_db),
):
    academic_year = data.get("academic_year")
    form          = data.get("form", {})
    totals        = data.get("totals", {})

    from src.crud.core import get_faculty_by_email
    from src.setup.dependencies import get_form_family
    user = await get_faculty_by_email(db, current_user.email)
    form_family = get_form_family(user.school) if user else "standard"

    try:
        # 1. Write form rows into normalized tables
        await shred_form(db, current_user.email, academic_year, form, form_family)

        # 2. Create/update declaration record
        from src.schema.core import DeclarationBase
        decl_data = DeclarationBase(
            faculty_email=current_user.email,
            academic_year=academic_year,
            part_a_total=totals.get("partATotal", 0),
            part_b_total=totals.get("partBTotal", 0),
            grand_total=totals.get("grandTotal", 0),
            status="Submitted",
        )
        await create_or_update_declaration(db, decl_data)

        # 3. Update or create snapshot
        result = await db.execute(select(AppraisalSnapshot).where(
            AppraisalSnapshot.faculty_email == current_user.email,
            AppraisalSnapshot.academic_year == academic_year,
        ))
        db_snapshot = result.scalar_one_or_none()
        if db_snapshot:
            db_snapshot.payload = data
            flag_modified(db_snapshot, "payload")  # tell SQLAlchemy the JSON changed
        else:
            db.add(AppraisalSnapshot(
                faculty_email=current_user.email,
                academic_year=academic_year,
                payload=data,
            ))

        await db.commit()

    except Exception as e:
        await db.rollback()
        raise HTTPException(status_code=500, detail=f"Submission failed: {str(e)}")

    return {"message": "Submitted successfully", "submitted_at": datetime.utcnow().isoformat()}
One more thing to check: after deploying this, if data still shows up as NULL in the tables (rows exist but all blank), that means the frontend field names don't match your model column names. For example, if TeachingProcess has a column subject_code but the frontend sends code, the hasattr check silently skips it.

To diagnose that, share your part_a.py and part_b.py model files and I'll add an explicit field-name mapping for each section.
=======

    return value
And update the field-setting loop inside shred_form:


for field_name, value in item.items():
    if not hasattr(db_item, field_name):
        continue
    coerced = _coerce_for_column(db_item, field_name, value)
    if coerced is None:
        continue  # let DB defaults apply
    setattr(db_item, field_name, coerced)
Also remove the old _safe_db_value function entirely — _coerce_for_column replaces it completely. The _safe_num helper for totals can stay as-is.
>>>>>>> refs/remotes/origin/main
